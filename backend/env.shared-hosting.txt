# Shared Hosting Environment Configuration (tenten)
# Copy this file to .env on your shared hosting

# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/minicde_db"
DIRECT_URL="postgresql://username:password@localhost:5432/minicde_db"

# Database Connection Pooling (reduced for shared hosting)
DATABASE_POOL_MIN=1
DATABASE_POOL_MAX=5
DATABASE_POOL_IDLE_TIMEOUT=30000
DATABASE_POOL_ACQUIRE_TIMEOUT=60000

# Redis Configuration (optional - may not be available on shared hosting)
# REDIS_URL="redis://localhost:6379"
# REDIS_PASSWORD=""
# REDIS_DB=0
# REDIS_TTL=3600

# JWT Configuration
JWT_SECRET="your_secure_jwt_secret_for_shared_hosting_2024"
JWT_EXPIRES_IN="7d"

# Server Configuration
PORT=3001
NODE_ENV=production
SHARED_HOSTING=true

# Performance Configuration (reduced for shared hosting)
MAX_FILE_SIZE=52428800 # 50MB
ALLOWED_FILE_TYPES="pdf,dwg,rvt,ifc,docx,xlsx,jpg,png"
REQUEST_TIMEOUT=30000
RESPONSE_TIMEOUT=30000

# Rate Limiting (reduced for shared hosting)
RATE_LIMIT_WINDOW_MS=900000 # 15 minutes
RATE_LIMIT_MAX_REQUESTS=500
AUTH_RATE_LIMIT_MAX=100

# File Upload Configuration
UPLOAD_PATH="./uploads"

# Email Configuration
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
EMAIL_FROM="CDE BIM <your-email@gmail.com>"

# CORS Configuration
CORS_ORIGIN="https://qlda.hoanglong24.com,https://www.qlda.hoanglong24.com"

# Feature flags
ENABLE_PROJECT_STATS=true
ENABLE_PROJECT_EXPORT=true
ENABLE_PROJECT_SHARING=true

# Cloudinary Configuration (for file storage)
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# Azure OneDrive Configuration (optional)
# AZURE_TENANT_ID="your-tenant-id"
# AZURE_CLIENT_ID="your-client-id"
# AZURE_CLIENT_SECRET="your-client-secret"
# AZURE_REFRESH_TOKEN="your-refresh-token"
